bin_PROGRAMS = sigmin-one

# sigmin-one
sigmin_one_SOURCES   = sigmin-one.c
sigmin_one_LDADD     = ../lts-lib/liblts-lib.la ../lts-io/liblts-io.la
sigmin_one_LDADD    +=../hre/libhre.la ../libltsmin.la
sigmin_one_CPPFLAGS  = -I ${top_srcdir}/src $(AM_CPPFLAGS)
sigmin_one_LDFLAGS   = $(AM_LDFLAGS)
